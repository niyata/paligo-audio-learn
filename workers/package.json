{
  "name": "paligo-inbox-api",
  "private": true,
  "version": "0.1.0",
  "description": "Paligo Exam Inbox API — Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler check",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
