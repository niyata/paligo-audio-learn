{
  "name": "@paligo/line-issue-bot-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Portable LINE webhook worker for creating GitHub issues across repos",
  "scripts": {
    "dev": "wrangler dev --port 8790 --ip 127.0.0.1",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
